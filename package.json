{
  "name": "addhelper",
  "version": "1.0.0",
  "description": "Tapahtumamarkkinoinnin työnkulkualusta: yksi tapahtuma sisään, kaikki kanavat ulos muutamalla klikkauksella.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1"
  }
}
